# LiteLLM / OpenAI-compatible endpoint (optional — only for graphify --with-docs / --label).
# Copy to .env and set the key your proxy expects. Do not commit .env.

OPENAI_API_KEY=replace-with-your-litellm-key

# Optional overrides (refresh-graph.sh defaults):
# OPENAI_BASE_URL=http://localhost:4000/v1
# OPENAI_MODEL=llama-3.3-70b-instruct
