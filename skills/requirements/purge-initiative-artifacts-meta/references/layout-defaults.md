# Layout defaults (`/purge-initiative-artifacts-meta`)

When `.harness/profile.yaml` is absent, use:

| Key | Default |
|-----|---------|
| `prd_root` | `prd/` |
| `reports_dir` | `prd/reports/` |

No purge report path — handoff only.
Allowlist SSOT: `../../../references/artifact-write-contract.md`.
